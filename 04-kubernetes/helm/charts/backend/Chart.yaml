apiVersion: v2
name: backend
description: A Helm chart for the Node.js backend service
type: application
version: 0.1.0        # Chart version — bump on chart changes
appVersion: "1.0.0"     # Version of the app this chart deploys
