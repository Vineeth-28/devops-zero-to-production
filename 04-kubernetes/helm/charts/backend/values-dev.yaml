# Dev environment overrides

replicaCount: 1

image:
  tag: "dev-latest"
  pullPolicy: Always

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 250m
    memory: 128Mi

ingress:
  enabled: false

configMap:
  NODE_ENV: development
  LOG_LEVEL: debug
