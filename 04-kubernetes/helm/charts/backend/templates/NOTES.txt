Backend release "{{ .Release.Name }}" has been deployed.

Chart:   {{ .Chart.Name }}-{{ .Chart.Version }}
Image:   {{ .Values.image.repository }}:{{ .Values.image.tag }}
Replicas: {{ .Values.replicaCount }}

{{- if .Values.ingress.enabled }}

Access the app at:
  http://{{ .Values.ingress.host }}{{ .Values.ingress.path }}
{{- else }}

Ingress is disabled. Access the app inside the cluster via:
  {{ include "backend.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}
{{- end }}

Useful commands:
  helm status {{ .Release.Name }}
  helm get values {{ .Release.Name }}
  kubectl get pods -l app=backend
